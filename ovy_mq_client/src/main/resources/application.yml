logging:
  level:
    root: INFO
    io.github.jotabrc.ovy_mq: TRACE
  pattern:
    console: "%date [%thread] [%-5level] %logger{40} - %message%n"

ovymq:
  task:
    consumer:
      active: true
      delay: 5000

spring:
  application.name: OVY_Mq_client
  config:
    import: classpath:credential.yml